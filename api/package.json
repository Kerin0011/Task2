{
  "name": "taskflowspa-api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "json-server --watch db.json --port 3000",
    "server": "npm run start"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.15"
  }
}
